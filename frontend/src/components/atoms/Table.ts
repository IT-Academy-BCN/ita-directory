import styled from 'styled-components'

const TableStyle = styled.table``

const THead = styled.thead``

const Th = styled.th``

const TBody = styled.tbody``

const Tr = styled.tr``

const Td = styled.td``

export { TableStyle, THead, Th, TBody, Tr, Td }
