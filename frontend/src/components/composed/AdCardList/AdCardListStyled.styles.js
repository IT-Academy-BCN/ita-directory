import styled from "styled-components";

export const AdCardListStyled = styled.div`
	position: relative;
	overflow: visible;

	.list-scroll {
		display: flex;
		flex-wrap: wrap;
		overflow: auto;
		position: relative;
		height: 100vh;
	}
`;
