//components
import Body from "components/layout/Body/Body";

const PageNotFound = () => {
	return (
		<Body>
			Ops, parece que la página que estas buscando no existe. Vuelve a la página de inicio. Si
			se trata de un error, ponte en contacto.
		</Body>
	);
};

export default PageNotFound;
