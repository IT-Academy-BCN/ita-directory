name: Frontend Deploy

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: build and deploy
      - uses: actions/checkout@v3
      — run: | 
          pwd
          cd scripts
          bash dev.deploy.sh